# REMOVED: sensitive environment variables were removed from this file for security reasons.
# If you need to run locally, create a .env file from .env.example and add your values.
# IMPORTANT: Any keys that were previously committed must be rotated immediately (Supabase, Google API keys, etc.).
